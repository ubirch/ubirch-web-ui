import { HeaderActionButton } from './header-action-button';

describe('HeaderActionButton', () => {
  it('should create an instance', () => {
    expect(new HeaderActionButton({})).toBeTruthy();
  });
});
