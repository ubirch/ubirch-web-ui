import { DevicesListWrapper } from './devices-list-wrapper';

describe('DevicesListWrapper', () => {
  it('should create an instance', () => {
    expect(new DevicesListWrapper()).toBeTruthy();
  });
});
