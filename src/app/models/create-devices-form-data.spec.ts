import { CreateDevicesFormData } from './create-devices-form-data';

describe('CreateDevicesFormData', () => {
  it('should create an instance', () => {
    expect(new CreateDevicesFormData({})).toBeTruthy();
  });
});
